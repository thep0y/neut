/** How many toasts are visible at once (stacked) */
export const VISIBLE_TOASTS_AMOUNT = 3;

/** Default desktop viewport offset */
export const VIEWPORT_OFFSET = "24px";

/** Default mobile viewport offset */
export const MOBILE_VIEWPORT_OFFSET = "16px";

/** Auto-dismiss duration in milliseconds */
export const TOAST_LIFETIME = 4000;

/** Default toast width in pixels */
export const TOAST_WIDTH = 356;

/** Vertical gap between stacked toasts in pixels */
export const GAP = 14;

/** Minimum swipe distance (px) or velocity to dismiss */
export const SWIPE_THRESHOLD = 45;

/** Delay (ms) before removing a dismissed toast from the DOM */
export const TIME_BEFORE_UNMOUNT = 200;
